
%header
  %h2.page-title
    .caption
      = page_title! 'Productos'

  - @bottles.each do |bottle|
    = link_to image_tag(bottle.bottle_url(:little)), product_url(bottle)

  = render 'carousel'

  //- if current_admin
  .administrate.create{:style => 'display: none'}
    = link_to 'Edit', edit_product_path(@product), class: 'button button-update'



.content
  .producto-imagen
    = image_tag @product.bottle_url(:regular) if @product.bottle?
  .producto-detalles
    .linea-vertical
      .linea-horizontal
    %h3.title
      .caption
        = truncate(@product.name, length: 25)
    %span.sub-title
      .caption
        = paragraph(@product.description)

    .producto-benefit
      %h4.box-title
        PRODUCT BENEFIT
      .producto-benefit-inner
        = paragraph(@product.content)


  


